#!/usr/bin/env node
// Generated by CoffeeScript 1.3.3
var argv, command, commands, fs, showUsage;

argv = require('optimist').argv;

fs = require('fs');

commands = ['dev', 'test', 'deploy'];

showUsage = function() {
  return fs.createReadStream(__dirname + '/usage.txt').pipe(process.stdout).on('close', function() {
    return process.exit(1);
  });
};

if (argv._.length) {
  command = argv._[0];
  if (commands.indexOf(command) !== -1) {
    console.log(command);
  } else {
    showUsage();
  }
} else {
  showUsage();
}
